# from django.urls import path
# from .views import EPPEventView, RealTimeEPPDetectionView

# urlpatterns = [
#     path('detect/', EPPEventView.as_view(), name='epp-detect'),
#     path('realtime/', RealTimeEPPDetectionView.as_view(), name='epp-realtime'),
# ]
